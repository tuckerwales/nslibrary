# Switch homebrew (M4)

CMake project for the NSLibrary installer `.nro`. Host-native tests (no devkitPro) live in `tests/` and parse the golden fixtures.

Build and usage: [docs/switch.md](../docs/switch.md).
