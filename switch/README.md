# Switch homebrew

CMake project for the NSLibrary installer `.nro`. Host-native tests (no devkitPro) live in `tests/` and parse the golden fixtures (PFS0, HFS0/XCI, NCZ, pipeline).

Build and usage: [docs/switch.md](../docs/switch.md).
